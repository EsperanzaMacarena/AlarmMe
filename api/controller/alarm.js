const _ = require('lodash');
const error_types = require('./error_types');
const Alarm = require ('../model/alarm');

module.exports = {

    newAlarm:(req,res)=>{
        let alarm = new Alarm({

        });

    }

}